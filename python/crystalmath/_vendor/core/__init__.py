"""Vendored ``core`` namespace (ADR-006, crystalmath-xi1).

Intentionally empty: unlike the original ``tui/src/core/__init__.py`` this does
not re-export submodules, to avoid eagerly importing non-vendored / UI code.
"""
